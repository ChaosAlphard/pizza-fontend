/**
 * discarded
 */
const getPizzaData = (state:any) => state.pizzaData

const getUserInfo = (state:any) => state.userInfo

const isLogin = (state:any) => state.isLogin

export {
  getPizzaData,
  getUserInfo,
  isLogin
}